require "spec_helper"

describe Purty::Color::Picker do
  it "has a version number" do
    expect(Purty::Color::Picker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
